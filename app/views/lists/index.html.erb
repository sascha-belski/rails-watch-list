<p style="color: green"><%= notice %></p>

<h1>Lists</h1>

<div id="lists">
  <% @lists.each do |list| %>
    <%= render list %>
    <p>
      <%= link_to "Show all bookmarks to this list", list %>
    </p>
  <% end %>
</div>

<%= link_to "New list", new_list_path %>